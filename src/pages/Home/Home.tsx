import Button from '@/components/common/Button/Button';

const Home = () => {
  return (
    <div>
      <div>Home</div>
      <Button size="md" shadow>
        버튼
      </Button>
      <div>THIS IS HOME PAGE.</div>
    </div>
  );
};

export default Home;
